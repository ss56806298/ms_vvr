# Microsoft voice virtual request

Read csv files and generate SSML files used to create AI voice files.

## How to use?
First step:
```
git submodule update --init --recursive
```
Second step:
```
Build.bat
```